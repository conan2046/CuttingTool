interface:
  display_name: "Game UI Asset Pipeline"
  short_description: "Orchestrate and package game UI bitmap assets"
  default_prompt: "Use $game-ui-asset-pipeline. On first use, resolve the project name and initialize its local references folder and reference-notes.md before turning my natural-language UI request into resumable generation jobs, transparent PNGs, QA, and a delivery summary."
