interface:
  display_name: "Game UI Asset Pipeline"
  short_description: "Orchestrate and package game UI bitmap assets"
  default_prompt: "Use $game-ui-asset-pipeline. Resolve only the missing project name, reference placement, and one consolidated confirmation of screen layout, canonical-element consistency, and pixel size; then automatically update reference notes, deduplicate reusable project assets, run the remaining stages without user pauses, and deliver transparent PNGs with Manifest and QA."
