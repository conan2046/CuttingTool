interface:
  display_name: "Game UI Asset Pipeline"
  short_description: "Orchestrate and package game UI bitmap assets"
  default_prompt: "Use $game-ui-asset-pipeline to turn my natural-language UI request into resumable generation jobs, create and split the asset sheets, validate transparent PNGs, and return the manifest, contact sheet, QA report, and delivery summary."
